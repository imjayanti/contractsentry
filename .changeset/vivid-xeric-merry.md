---
"@contractsentry/core": minor
---

Auto-detect TypeScript framework routes (Express, Hono, Fastify) from inline handler call expressions — no `// @route` annotation required. Express `:param` segments are automatically converted to OpenAPI `{param}` format.
